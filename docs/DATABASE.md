# Database

Status: full domain schema implemented (Phase 2), unverified against a live
database (see `PROJECT_ANALYSIS.md` §10/§11).

See `PROJECT_ANALYSIS.md` §4 for the model table, design notes, and
migration status — that document is the current source of truth, same
reasoning as `ARCHITECTURE.md`: one place to update per phase instead of
two documents drifting out of sync.
