# Architecture

See `PROJECT_ANALYSIS.md` §1–3 for the current state (updated every phase).
This file becomes the stable long-form reference once the architecture
settles past Phase 6; until then `PROJECT_ANALYSIS.md` is the source of
truth to avoid keeping two documents in sync during active scaffolding.
